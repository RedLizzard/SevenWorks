{{#each entries}}
    <div class="card card-body mb-2">
        <h4>{{title}}</h4>
        <p>{{details}}</p>
        <a class="btn btn-dark btn-block" href="/entries/edit/{{id}}">Edit</a>
    </div>
{{else}}
    <p>No entries listed</p>
{{/each}}